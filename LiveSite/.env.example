# Backend API URL (for future integration)
# For local development, use: http://localhost:5000
# For production with local backend during demo, use your local IP: http://192.168.x.x:5000
# Leave empty to use localStorage only (current setup)
VITE_API_URL=
